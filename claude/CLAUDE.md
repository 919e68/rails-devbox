# Global Instructions

## Git
- Do not add Claude attribution to git commits or pull requests (no `Co-Authored-By: Claude` trailer, no "Generated with Claude Code" lines in commit messages or PR descriptions).

## Cloning repositories
- Always clone repositories into the `~/apps` folder (`/home/dev/apps`), e.g. `git clone <url> ~/apps/<repo-name>`.
